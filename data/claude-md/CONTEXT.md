# CONTEXT.md — provider-degradation manifest (generated)

<!-- GENERATED by data/scripts/generate-root-contract.py from data/facts/catalog.json — do NOT hand-edit. Regenerate: python3 data/scripts/generate-root-contract.py -->

These capabilities use Claude-Code-only constructs (the Agent / AskUserQuestion / Workflow tools). On Codex and OpenCode they render with the degradation noted below rather than being silently dropped. Everything else in the kit is fully provider-neutral.

**5 provider-partial items** (derived live from the catalog):

- **entrypoint/memory-sync** — Uses Claude-only capability: Agent. Providers without an explicit reviewed adapter render this degraded.
- **entrypoint/milestone-pipeline** — Uses Claude-only capability: Agent, Workflow. Providers without an explicit reviewed adapter render this degraded.
- **entrypoint/roadmap** — Uses Claude-only capability: Agent. Providers without an explicit reviewed adapter render this degraded.
- **entrypoint/skill-to-command** — Uses Claude-only capability: Agent. Providers without an explicit reviewed adapter render this degraded.
- **entrypoint/spike** — Uses Claude-only capability: Agent. Providers without an explicit reviewed adapter render this degraded.
