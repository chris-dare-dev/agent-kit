{
  "_TODO_PENDING_LIVE_VERIFICATION": "STUB — the 'mcp' key name and block shape follow https://opencode.ai/config.json but are UNCONFIRMED against a live OpenCode session, and nothing plants this file yet. Wiring the plant is tracked in the providers-as-data milestone. What is NOT provisional: the server registered here is this kit's own, launched from ${AGENT_KIT_ROOT}/dist/index.js, matching the Claude and Codex templates.",
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["./.opencode/plugin/secret-guard.js"],
  "mcp": {
    "agent-kit": {
      "type": "local",
      "command": ["node", "${AGENT_KIT_ROOT}/dist/index.js"],
      "environment": {
        "SERVER_PROFILE": "personal"
      }
    }
  }
}
