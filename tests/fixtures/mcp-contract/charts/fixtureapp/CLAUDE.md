# fixtureapp (chart)

CHART-FIXTURE-UNIQUE-MARKER — content that lives ONLY in the chart-side CLAUDE.md.

## Pitfalls
- This marker proves the chart CLAUDE.md is indexed distinctly from the source one
  (S1.1b re-key). Pre-fix, source/ silently overwrote charts/ and this file vanished.
