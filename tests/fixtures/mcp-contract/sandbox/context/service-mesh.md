# Service Mesh (fixture context guide)

Fixture context guide whose filename is hyphenated (service-mesh.md) — discovery
derives the topic "service mesh". Proves get_context_guide("service-mesh") (the
hyphenated form every CLAUDE.md tier documents) resolves after canonicalization.
